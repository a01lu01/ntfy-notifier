# 2026-04-19 工作日志

## ntfy-Notifier 代码审查（v1）
- 完成全项目代码检查，输出 `检查文档.md`
- 发现 4 个致命问题：notifier.py 缺 import json、src/ 缺 __init__.py、tkinter/Win32 线程冲突、winrt 依赖未声明
- 发现 5 个严重问题：明文密码硬编码、日志路径硬编码、Python 3.10+ 语法、轮询重复拉取历史消息、test_ui.py 引用不存在模块
- 发现 7 个一般问题：pystray 未使用、DLL 版本硬编码、console=True、菜单分隔线、update() 空实现、日志无轮转、全局变量无锁

## ntfy-Notifier 代码审查（v2，用户修改后复查）
- 用户已修复：import json、__init__.py、密码改空、日志路径移除
- 新发现关键问题：pywin32 未安装（只有 pywin32-ctypes）、winrt 未安装
- 新发现代码问题：_mkicon() GDI 泄漏、WM_COMMAND 写成 WM_SYSCOMMAND(0x112)、szTip 字段类型错误、MSG 结构体 64 位不兼容、_pump 空转耗 CPU
- 建议最快跑通：pip install pywin32 winsdk，然后用 pystray 重写 tray.py
